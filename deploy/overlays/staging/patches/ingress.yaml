apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mob-api
  namespace: mob
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: letsencrypt-staging
spec:
  tls:
    - hosts:
        - staging.mob.example.com
      secretName: mob-staging-tls
  rules:
    - host: staging.mob.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mob-api
                port:
                  number: 8080
