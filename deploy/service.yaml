apiVersion: v1
kind: Service
metadata:
  name: mob-api
  namespace: mob
  labels:
    app.kubernetes.io/name: mob
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mob
    app.kubernetes.io/component: api
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
