apiVersion: v1
kind: Service
metadata:
  name: mob-litellm
  namespace: mob
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: litellm
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
