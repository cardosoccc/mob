apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mob-api
  namespace: mob
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: mob.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mob-api
                port:
                  number: 8080
