FROM python:3.11-slim

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn httpx kubernetes

COPY src/mob/webhook/ /app/mob/webhook/

EXPOSE 8082

ENTRYPOINT ["python", "-m", "mob.webhook.gateway"]
